import { useState } from "react"

const Cinema = () =>{

    return(
        <button onClick={}>
            <image></image>
            <div>title</div>
        </button>
        
    )
}
export default Cinema